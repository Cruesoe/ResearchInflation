<Project Sdk="Microsoft.NET.Sdk">
  <!-- Build settings, RimWorld references and the copy into 1.6\Assemblies all come
       from _ModKit via the Directory.Build.props/.targets at the repo root. -->
  <PropertyGroup>
    <RootNamespace>ResearchInflation</RootNamespace>
    <AssemblyName>ResearchInflation</AssemblyName>
    <UnityModules>CoreModule;IMGUIModule;InputLegacyModule;TextRenderingModule</UnityModules>
  </PropertyGroup>
</Project>
